// 'use client';

// import { useAuthStore } from "@/store/useAuthStore";
// import { useFavorite } from "@/hooks/realtime/useFavorite";

// export function EventList() {
//     const { user } = useAuthStore();
//     const { favorites, addFavorite, removeFavorite, isFavorite } = useFavorite(user?.uid || '');

//     const handleFavoriteClick = async (playerId: string) => {
//         if (!user) return;

//         if (isFavorite( playerId)) {

//     }
// }
